// import React from 'react'
// import type { RootState } from '../store'
import { useSelector } from 'react-redux'

export  default function Search() {
   // const data = useSelector((state: RootState) => state)
  
  return (
    <div>
      <div>List All Data
       {/* {data.form.map((a) => <p>a</p>)} */}
      </div>
    </div>
  )
}